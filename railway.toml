# https://docs.railway.com/reference/config-as-code
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["source/**", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
